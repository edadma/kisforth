# Shared application code - not a library, just source definitions
set(KISFORTH_SHARED_SOURCES
        ${CMAKE_CURRENT_LIST_DIR}/src/startup.c
)

set(KISFORTH_SHARED_INCLUDES
        ${CMAKE_CURRENT_LIST_DIR}/include
)